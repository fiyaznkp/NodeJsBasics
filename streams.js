// it is moving of data from on epoint to another over time



// buffer server waits the client for processing data

// buffer

// const buffer = new Buffer.from("fiyaz")

// console.log(buffer)

// const buffer = new Buffer.from("fiyaz")

// console.log(buffer.toString())

// const buffer = new Buffer.from("fiyaz")

// console.log(buffer.toJSON())